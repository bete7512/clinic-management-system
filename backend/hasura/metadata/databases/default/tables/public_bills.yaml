table:
  name: bills
  schema: public
insert_permissions:
  - role: admins
    permission:
      check: {}
      columns:
        - due_date
        - total
        - payment_for
        - bill_id
        - d_id
        - p_id
  - role: recieptionists
    permission:
      check: {}
      set:
        recieptionist: x-hasura-User-Id
      columns:
        - due_date
        - total
        - payment_for
        - bill_id
        - d_id
        - p_id
select_permissions:
  - role: admins
    permission:
      columns:
        - due_date
        - total
        - payment_for
        - bill_id
        - d_id
        - p_id
      filter: {}
update_permissions:
  - role: admins
    permission:
      columns:
        - due_date
        - total
        - payment_for
        - bill_id
        - d_id
        - p_id
      filter: {}
      check: {}
delete_permissions:
  - role: admins
    permission:
      backend_only: false
      filter: {}
